[config]
model = "vertex_ai/gemini-3.1-pro-preview"
max_model_tokens = 32000

[pr_reviewer]
extra_instructions = """
- Focus on logic, edge cases, null pointer, and architectural alignment.
- Flag hardcoded secrets or environment variables.
- Ensure new public methods have documentation comments.
- Provide code suggestion to fix issues
"""
inline_code_comments = true